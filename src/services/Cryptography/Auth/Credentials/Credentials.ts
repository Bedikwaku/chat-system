export abstract class Credentials {}
// We use an abstract class here instead of an interface to highlight the fact that
// this class is not meant to be instantiated directly, but rather extended by other classes.
